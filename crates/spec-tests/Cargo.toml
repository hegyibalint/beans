[package]
name = "spec-tests"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[lib]
name = "beans_spec_tests"

[dependencies]
core = { path = "../core" }
engine = { path = "../engine" }
platform-jvm = { path = "../platform-jvm" }
