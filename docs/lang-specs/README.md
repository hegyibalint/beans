Run `python3 scripts/specs/update.py` (requires pandoc) to download the pinned JLS, JVMS, and LSP specifications as Markdown. Run `python3 scripts/specs/lsp.py` to download only the LSP spec.

Generated texts under `jls/`, `jvms/`, and `lsp/` are not committed.
