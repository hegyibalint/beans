We are planning out the software for the time being.
You don't need to write functional code.
Write only focused snippets, and work with your peer step-by-step.

This is a fresh implementation. You can find our previous attempt on the `spike` branch.

The `docs/ARCHITECTURE.md` document should not be edited directly.
Propose changes as text in the conversation; only touch the file when explicitly permitted.

Comments should be used very sparingly; this is an experimental project.
