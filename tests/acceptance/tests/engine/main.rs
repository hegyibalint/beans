//! What Beans invented for itself, rather than what a language specification
//! asked for. Nothing here is a fact about Java; it is all bookkeeping that a
//! language server needs and that no spec describes.

mod classpath;
mod scoping;
