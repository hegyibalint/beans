package com.example.app;

/** Compiled by a source set the stock IDEA model folds into the test scope. */
public final class AppIntegrationTest {

    void prints() {
        App.main(new String[0]);
    }
}
